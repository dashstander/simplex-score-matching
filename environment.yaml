name: ssm
channels:
  - huggingface
  - conda-forge
  - defaults
dependencies:
  - python>=3.9
  - pip>=22
  - matplotlib
  - pytorch
  - seaborn
  - transformers
  - datasets
  - jupyterlab
  - ipykernel
  - ipywidgets
  - pip:
    - chex
    - einops
    - flax
    - gcsfs
    - jax
    - optax
    - pytest
    - tqdm
    - wandb
